# 曲线的曲率

### 这是一个及其重要的概念，可以被牵引到非欧几何->拓扑学->高斯的曲面曲率等高深的数学中。同时也给欧拉无穷分析引论的下册学习找了一个动力，欧拉没有用微积分的高级工具解决这些问题，将会是最好的教材，一共包含了149章图形，一路顺着学习就好（用描点会图形的方法），确实需要为人类做贡献，给孩子们假设一个具象的梯子。

![](/images/无穷和与无穷乘积/欧拉无穷分析引论下册中的典型推演实验/基本概念/曲线的曲率/1a1.jpg)

## 打开实验文件

### 在线调试环境

- 单击右方的[Jupyter Notebook](https://mybinder.org/v2/gh/ipython/ipython-in-depth/master?filepath=binder/Index.ipynb)，稍后在浏览器里会显示Jupyter Notebook的运行环境。
- 在File的第一个下拉菜单“New Notebook” 的右侧箭头处选择“Python 3”，然后会显示一个新的页面
- 把下面的这段python代码拷贝到这个页面“In [ ]:”右侧的空白栏中， 然后单击上方的按键“运行”。

```python
import numpy as np
import matplotlib.pyplot as plt

# 定义函数
def F(x, y):
    return y**3 + x * y**2 + x**2 * y + 6

# 创建网格
x = np.linspace(-5, 5, 400)
y = np.linspace(-5, 5, 400)
X, Y = np.meshgrid(x, y)
Z = F(X, Y)

# 画等高线 F=0
plt.figure(figsize=(8, 6))
plt.contour(X, Y, Z, levels=[0], colors='blue', linewidths=2)
plt.axhline(0, color='black', linewidth=0.5)
plt.axvline(0, color='black', linewidth=0.5)
plt.xlabel('x')
plt.ylabel('y')
plt.title('Curve: $y^3 + xy^2 + x^2 y + 6 = 0$')
plt.grid()
plt.axis('equal')
plt.show()
```

1. 维基百科
	- [Curvature](https://en.wikipedia.org/wiki/Curvature) | [曲率](https://zh.wikipedia.org/wiki/曲率) 
	- [Osculating circle](https://en.wikipedia.org/wiki/Osculating_circle)